{
  "name": "EchoChamber",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Aaron Cohen",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently \"yarn workspace server dev\" \"yarn workspace client start\" -n \"EchoChamber,React\"",
    "react:build": "yarn workspace client build",
    "deploy": "concurrently --kill-others-on-fail \"yarn workspace server start\" \"yarn workspace client deploy\" -n \"EchoChamber,React\""
  },
  "devDependencies": {
    "concurrently": "^8.2.1",
    "prettier": "^3.0.3"
  }
}
